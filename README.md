# Лабораторная работа №2: CI/CD с GitHub Actions

## 📌 Описание
В данной лабораторной работе реализован автоматический деплой статического сайта с использованием **GitHub Actions**. При каждом пуше в репозиторий и создании Pull Request запускается CI-пайплайн, проверяющий корректность HTML-кода. При создании **Release** автоматически выполняется деплой на удалённый сервер через SSH.

## 🚀 Функциональность

### CI (Continuous Integration)
- Проверка наличия эмодзи 🚀 в `index.html`
- Валидация HTML-синтаксиса
- Проверка на пустые атрибуты (`href=""`, `src=""`)
- Проверка структуры HTML-документа
- Линтинг HTML (опционально)

### CD (Continuous Deployment)
- Автоматический деплой при создании Release
- Копирование файлов на сервер через SSH
- Настройка nginx и перезагрузка конфигурации
- Проверка доступности сайта после деплоя

## 📁 Структура работы
```
.
├── .github/workflows/
│   ├── ci.yml          # CI-пайплайн (проверка кода)
│   └── deploy.yml      # CD-пайплайн (деплой при релизе)
├── index.html           # Демо-сайт с ракетой 🚀
├── nginx.conf           # Конфигурация nginx (порт 8181)
├── deploy.sh            # Скрипт ручного деплоя
└── README.md            # Этот файл
```

